# Production Environment Configuration
# Copy this file to .env and update all values

# Flask Environment - MUST be 'production' for production deployments
FLASK_APP=app.py
FLASK_ENV=production

# Secret Key - REQUIRED: Generate a strong random key
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-256-bit-secret-key-here-change-this

# Admin Password - REQUIRED: Change from default
ADMIN_PASSWORD=your-strong-admin-password-here

# Base URL - Your production domain with HTTPS
BASE_URL=https://quiz.yourdomain.com

# PostgreSQL Database Connection
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://quiz_user:your-db-password@localhost:5432/quiz_app

# Optional: Rate Limiting Storage (Redis recommended for production)
# RATELIMIT_STORAGE_URI=redis://localhost:6379/0
